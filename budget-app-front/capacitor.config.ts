import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.budgetapp.frontend',
  appName: 'budget-app-front',
  webDir: 'www',
};

export default config;
